/* =========================================================
   FAEZAPROJECT V2
   APPLICATION
   ========================================================= */

(function () {
    "use strict";

    document.addEventListener(
        "DOMContentLoaded",
        function () {

            document.documentElement.classList.add(
                "js-ready"
            );

            console.log(
                "FaezaProject V2 siap."
            );

        }
    );

})();
